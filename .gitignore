# Dependencies
node_modules/

# Skill install artifacts (npx skills add, git submodule)
# Only installed skills are ignored. The tracked brand and ecommerce context
# files in .agents/*.md are part of this repo (see .agents/README.md).
.agents/skills/
.agents/marketingskills/
.claude/
skills-lock.json

# Client data must never be committed (exports, analytics, customer lists)
*.csv
!skills/**/*.csv
!tools/**/*.csv
data/
exports/

# Environment variables / secrets
.env
.env.*
!.env.example

# macOS
.DS_Store
**/.DS_Store

# macOS / iCloud duplicate files
* 2.*
* 2/

# Remotion video project (root only, not skills/video/)
/video/

# Editor
*.swp
*.swo
*~
.idea/
.vscode/
.partners-pending/
